import "@/features/social-engine/styles/brand-tokens.css";
import type { ReactNode } from "react";

export default function InternalCarouselLayout({ children }: { children: ReactNode }) {
  return children;
}
